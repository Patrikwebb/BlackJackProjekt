package kodaLoss;

public enum CasinoRules {
/** split the hand of two equal ranks in two hands, playing
 *  both with the bet amount by same player*/
  SPLIT,
  
  /** Double the bet and get one extra card in this round*/
  DOUBLE;
}
